# 服装商城 CloudBase 云开发迁移方案

> 基于 CloudBase 能力边界调研 + 当前项目后端全景分析，2026-04-08

---

## 1. 现状概览

| 维度 | 当前状态 |
|------|---------|
| 后端 | Spring Boot 2.1.5 + Java 8 + MyBatis，部署在阿里云 ECS Docker |
| 数据库 | MySQL 8.0，阿里云 ECS 自建实例 |
| 存储 | 阿里云 OSS |
| 小程序 | 原生微信小程序，`wx.request` 调 REST API |
| 管理后台 | Vue 2 + Element UI，Shiro + Session 认证 |
| API 总量 | **328 个端点**（Admin 206 + WX 122） |
| 业务模块 | 37 个 Admin Controller + 27 个 WX Controller |
| 第三方集成 | 企业微信、腾讯云短信、火山引擎 Doubao Vision（AI 识别）、阿里云市场物流查询 |
| 数据模型 | 71 个 Domain 类、43 个 Mapper、42 个 Service |

---

## 2. 迁移目标架构

```
┌─────────────────────────────────────────────────────────┐
│                    CloudBase 环境                        │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  Node.js     │  │  Node.js     │  │  Node.js     │  │
│  │  云函数组     │  │  云函数组     │  │  云函数组     │  │
│  │  (WX 面向)   │  │  (Admin 面向) │  │  (定时任务)   │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
│         │                 │                  │          │
│         ▼                 ▼                  ▼          │
│  ┌─────────────────────────────────────────────────┐   │
│  │              CloudBase MySQL 8.0                 │   │
│  │         (表结构直迁，保留原有设计)                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌──────────────┐  ┌──────────────────────────────┐    │
│  │  云存储 (COS) │  │  静态托管 (Vue 管理后台)      │    │
│  └──────────────┘  └──────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘

调用关系：
  微信小程序 ──wx.cloud.callFunction()──► 云函数组 (WX) ──► MySQL
  管理后台 Vue ──JS SDK callFunction()──► 云函数组 (Admin) ──► MySQL
  （管理后台通过 CloudBase 静态托管默认域名访问，低频使用，无需自定义域名）
```

---

## 3. 技术选型决策

### 3.1 后端：Node.js 云函数（非 CloudRun）

| 决策 | 理由 |
|------|------|
| 语言选 Node.js 18 | 云函数一等公民，生态最好，冷启动优于 Java |
| 不用 CloudRun | 用户明确不希望走云托管 |
| 不用 Java 云函数 | JVM 冷启动 5-10 秒，不可接受 |

### 3.2 数据库：CloudBase MySQL（非 NoSQL）

| 决策 | 理由 |
|------|------|
| 保持 MySQL | 71 个 Domain 类的电商系统，关联查询复杂，NoSQL 不适合 |
| NoSQL 100 集合上限 | 当前 40+ 张表 + 关联，上限吃紧 |
| 表结构直迁 | 避免重写数据层，降低迁移风险 |
| 可行性高 | CloudBase MySQL 基于 TDSQL-C，兼容 MySQL 8.0 |

### 3.3 认证方案

| 端 | 当前 | 迁移后 |
|---|------|--------|
| 小程序 | JWT（自签） | CloudBase 自动注入 OPENID，云函数端 `cloud.getWXContext()` 获取 |
| 管理后台 | Shiro + Session | CloudBase Auth v2（用户名/密码登录）+ JS SDK |

### 3.4 存储：CloudBase 云存储

| 当前 | 迁移后 |
|------|--------|
| 阿里云 OSS | CloudBase 云存储（底层 COS） |

### 3.5 管理后台部署

| 当前 | 迁移后 |
|------|--------|
| Nginx 反代 + Docker | CloudBase 静态托管，Vue SPA 构建后上传 |

---

## 4. 云函数架构设计

### 4.1 分组策略

采用「按业务域分组」策略，而非一接口一函数，原因：
- 减少冷启动概率
- 集中 MySQL 连接管理
- 单函数代码包更紧凑

### 4.2 WX 端云函数（10 个函数）

| 函数名 | 职责 | 对应 Controller | 接口数 |
|--------|------|----------------|--------|
| `wx-auth` | 登录注册、验证码、用户信息 | WxAuthController | 12 |
| `wx-goods` | 商品列表、详情、搜索、分类 | WxGoodsController, WxCatalogController, WxSearchController, WxBrandController | 19 |
| `wx-cart` | 购物车 CRUD | WxCartController | 9 |
| `wx-order` | 下单、退款、售后全流程（支付后续单独接入） | WxOrderController, WxAftersaleController | 17 |
| `wx-user` | 地址、收藏、足迹、反馈 | WxAddressController, WxCollectController, WxFootprintController, WxFeedbackController | 14 |
| `wx-home` | 首页数据、广告、专题、公告 | WxHomeController, WxTopicController, WxIssueController, WxMsgController | 12 |
| `wx-coupon` | 优惠券领取、列表、兑换 | WxCouponController | 6 |
| `wx-clothing` | 服装 SKU、门店、导购、会员 | WxClothingSkuController, WxClothingStoreController, WxClothingUserController | 14 |
| `wx-storage` | 文件上传下载 | WxStorageController | 4 |
| `wx-tracker` | 埋点上报 | WxTrackerController | 2 |
| `wx-ai` | 吊牌识别、主图识别（火山引擎 Doubao Vision） | WxAiController（新） | 2 |
| `wx-express` | 物流查询（阿里云市场 API） | WxExpressController（新） | 1 |

### 4.3 Admin 端云函数（11 个函数）

| 函数名 | 职责 | 对应 Controller | 接口数 |
|--------|------|----------------|--------|
| `admin-auth` | 管理员登录、权限、角色 | AdminAuthController, AdminAdminController, AdminRoleController | 23 |
| `admin-goods` | 商品 CRUD、品牌、类目 | AdminGoodsController, AdminBrandController, AdminCategoryController | 19 |
| `admin-order` | 订单管理、退款、发货 | AdminOrderController, AdminAftersaleController | 22 |
| `admin-user` | 用户管理、会员 | AdminUserController | 4 |
| `admin-marketing` | 优惠券、限时特卖、满减 | AdminCouponController, AdminFlashSaleController, AdminFullReductionController | 19 |
| `admin-content` | 广告、专题、公告、反馈、FAQ | AdminAdController, AdminTopicController, AdminNoticeController, AdminFeedbackController, AdminIssueController, AdminKeywordController | 34 |
| `admin-config` | 系统配置管理 | AdminConfigController | 13 |
| `admin-storage` | 文件存储管理 | AdminStorageController | 6 |
| `admin-stat` | 统计、仪表盘、埋点分析 | AdminStatController, AdminDashbordController, AdminHistoryController | 13 |
| `admin-clothing` | 服装 SKU、门店、导购、会员等级 | AdminClothingSkuController, AdminClothingStoreController, AdminClothingGuideController, AdminClothingMemberLevelController | 26 |

**注意**：`AdminOcrController` 和 `WxOcrController` 不迁移（OCR 已弃用，改用 AI 识别）。
| `admin-wework` | 企业微信推送 | AdminWeWorkController, AdminOpsController | 7 |

### 4.4 定时任务云函数（2 个函数）

| 函数名 | 职责 | Cron |
|--------|------|------|
| `task-order` | 超时未支付自动取消（当前 Task/TaskService） | `0 */5 * * * *`（每 5 分钟） |
| `task-notify` | 定期通知（生日券等） | `0 0 9 * * *`（每天 9 点） |

### 4.5 共享层（Layer）

将公共依赖抽取为共享层，减少各函数代码包体积：

| 层名 | 内容 |
|------|------|
| `layer-base` | mysql2、数据库连接池、工具函数、响应格式化 |
| `layer-auth` | 认证鉴权中间件（OPENID 解析、Admin 权限校验） |
| `layer-wechat` | 企业微信 API 封装、火山引擎 AI 调用封装 |

### 4.6 单个云函数内部结构（示例）

```
wx-order/
├── index.js          # 入口，路由分发
├── service/
│   ├── order.js      # 订单业务逻辑
│   ├── payment.js    # 支付逻辑
│   └── aftersale.js  # 售后逻辑
└── package.json
```

**入口文件示例**：
```javascript
const { getOrderRoutes } = require('./service/order')
const { getPaymentRoutes } = require('./service/payment')

exports.main = async (event, context) => {
  const { action, data } = event
  const routes = {
    ...getOrderRoutes(),
    ...getPaymentRoutes(),
  }

  const handler = routes[action]
  if (!handler) {
    return { errno: 404, errmsg: `Unknown action: ${action}` }
  }
  return handler(data, context)
}
```

**小程序调用示例**：
```javascript
// 之前
wx.request({ url: '/wx/order/submit', method: 'POST', data: {...} })

// 之后
wx.cloud.callFunction({
  name: 'wx-order',
  data: { action: 'submit', data: {...} }
})
```

---

## 5. 数据库迁移方案

### 5.1 迁移策略：表结构直迁

| 步骤 | 操作 | 工具 |
|------|------|------|
| 1 | 阿里云 MySQL 导出 | `mysqldump -u root -p clothing_mall > backup.sql` |
| 2 | CloudBase 创建 MySQL 实例 | MCP: `manageSqlDatabase(action=provisionMySQL)` |
| 3 | 导入 SQL 到 CloudBase MySQL | DMC 平台 或 MCP: `manageSqlDatabase(action=initializeSchema)` |
| 4 | 验证表结构和数据完整性 | MCP: `querySqlDatabase(action=runQuery)` |

### 5.2 表结构调整

- 移除 Shiro 相关表（`litemall_admin` 的 `session_id` 等字段改用 CloudBase Auth）
- JWT 相关字段不再需要
- 新增 CloudBase 相关字段（如 `openid` 映射到 `litemall_user`）
- 存储表中 OSS URL 需批量更新为云存储 fileID

### 5.3 云函数连接 MySQL

```javascript
// layer-base 中提供共享连接池
const mysql = require('mysql2/promise')

let pool = null
function getPool() {
  if (!pool) {
    pool = mysql.createPool({
      host: process.env.MYSQL_HOST,
      port: parseInt(process.env.MYSQL_PORT || '3306'),
      user: process.env.MYSQL_USER,
      password: process.env.MYSQL_PASSWORD,
      database: process.env.MYSQL_DATABASE,
      connectionLimit: 5,
      waitForConnections: true,
    })
  }
  return pool
}
```

---

## 6. 第三方集成迁移

### 6.1 企业微信

| 当前 | 迁移后 |
|------|--------|
| HTTP 直调企微 API（主动调用） | 云函数 `axios`/`node-fetch` 出公网调用（能力不变） |
| 无回调 | 保持不变（只保留主动调用） |

### 6.2 短信通知

| 当前 | 迁移后 |
|------|--------|
| 腾讯云 SMS SDK（Java） | 腾讯云 SMS SDK（Node.js），API 不变 |

### 6.3 AI 吊牌/主图识别

| 当前 | 迁移后 |
|------|--------|
| 火山引擎 Doubao Vision API（OpenAI 兼容格式） | Node.js 中直接 `fetch` 调用同一 API，配置不变 |
| `TagRecognitionService`（吊牌识别） | 云函数 `wx-ai` 中实现 |
| `ImageRecognitionService`（主图识别） | 云函数 `wx-ai` 中实现 |
| 配置：`litemall.ai.tag.*` | 云函数环境变量：`DOUBAO_API_KEY`、`DOUBAO_ENDPOINT`、`DOUBAO_MODEL` |

### 6.4 物流查询

| 当前 | 迁移后 |
|------|--------|
| 阿里云市场 API（`qryexpress.market.alicloudapi.com`） | Node.js `fetch` 调用同一 API，APPCODE 鉴权方式不变 |
| 旧代码残留快递鸟实现（`ExpressService.java`，已弃用） | 不迁移 |

### 6.5 存储迁移

| 当前 | 迁移后 |
|------|--------|
| 阿里云 OSS | CloudBase 云存储（COS） |
| `AliyunStorage.java` | 小程序端 `wx.cloud.uploadFile`，Web 端 `app.uploadFile` |
| OSS URL 格式 | fileID 格式 `cloud://env-id/path` |

---

## 7. 前端改造方案

### 7.1 小程序改造

**改动范围：小**。核心变化：

1. `app.js` 增加云开发初始化
2. 封装 `cloudRequest` 工具函数替代 `util/api.js` 中的 `wx.request`
3. 登录流程改为 CloudBase 身份体系
4. 文件上传改为 `wx.cloud.uploadFile`

```javascript
// 改造前 (util/api.js)
function request(url, data) {
  return new Promise((resolve, reject) => {
    wx.request({
      url: WxApiRoot + url,
      method: 'POST',
      data: data,
      header: { 'X-Litemall-Token': token },
      success: res => resolve(res.data),
      fail: reject
    })
  })
}

// 改造后
function cloudCall(funcName, action, data) {
  return wx.cloud.callFunction({
    name: funcName,
    data: { action, data }
  }).then(res => res.result)
}
```

### 7.2 管理后台改造

**改动范围：大**。核心变化：

1. **安装 CloudBase JS SDK**：`npm install @cloudbase/js-sdk`
2. **初始化 CloudBase**：
   ```javascript
   import cloudbase from '@cloudbase/js-sdk'
   const app = cloudbase.init({
     env: 'your-env-id',
     accessKey: 'your-publishable-key',
   })
   ```
3. **认证替换**：Shiro + Session → CloudBase Auth（用户名/密码）
4. **API 调用替换**：`axios` → `app.callFunction()`
5. **文件上传替换**：OSS 上传 → `app.uploadFile()`
6. **路由改为 Hash 模式**：适配静态托管 SPA
7. **构建配置**：`publicPath` 改为相对路径

---

## 8. 认证迁移详解

### 8.1 小程序端

```
当前流程：
  wx.login() → code → POST /wx/auth/login_by_weixin → Java 获取 openid → 返回 JWT Token

迁移后流程：
  wx.cloud.callFunction() → 云函数通过 cloud.getWXContext() 直接拿到 OPENID → 无需手动登录

  首次使用：OPENID → 查询/创建 litemall_user → 返回用户信息
  后续使用：OPENID → 直接查询 litemall_user
```

### 8.2 管理后台

```
当前流程：
  验证码 → 用户名+密码 → Shiro Session → Cookie

迁移后流程：
  CloudBase Auth.signInWithPassword({ username, password })
  → 获得 access_token → JS SDK 自动携带到后续 callFunction
  → 云函数端通过 auth.getUserInfo() 获取调用者身份 → 权限校验
```

### 8.3 Admin 权限体系保留

现有的 RBAC（用户 → 角色 → 权限）模型在云函数端保留：
- `admin-auth` 云函数内实现权限校验中间件
- 其他 Admin 云函数在入口统一鉴权
- 数据库存 `litemall_role`、`litemall_permission` 表结构不变

---

## 9. 实施阶段

### 阶段一：基础设施搭建（1-2 天）

| 任务 | 说明 |
|------|------|
| 1.1 创建 CloudBase 环境 | 通过 MCP 或控制台 |
| 1.2 创建 MySQL 实例 | `manageSqlDatabase(action=provisionMySQL)` |
| 1.3 迁移数据库 | 导出 → 导入，验证表结构 |
| 1.4 创建共享层 | layer-base（MySQL 连接池、工具函数） |
| 1.5 配置环境变量 | MySQL 连接信息、微信支付密钥等 |

### 阶段二：核心 WX 云函数（优先级最高，3-5 天）

按优先级顺序开发：

| 顺序 | 云函数 | 理由 |
|------|--------|------|
| 1 | `wx-auth` | 身份是一切的基础 |
| 2 | `wx-goods` | 商品浏览是核心功能 |
| 3 | `wx-home` | 首页是入口 |
| 4 | `wx-cart` | 购物车是转化关键 |
| 5 | `wx-order` | 订单 + 支付是核心闭环 |
| 6 | `wx-user` | 地址、收藏等用户功能 |
| 7 | `wx-coupon` | 营销功能 |
| 8 | `wx-clothing` | 服装特色功能 |
| 9 | `wx-storage` | 文件上传 |
| 10 | `wx-tracker` | 埋点 |

### 阶段三：小程序适配（2-3 天）

| 任务 | 说明 |
|------|------|
| 3.1 初始化云开发 SDK | `wx.cloud.init()` |
| 3.2 封装 cloudCall 工具 | 替代 `wx.request` |
| 3.3 逐页面替换 API 调用 | 从首页开始 |
| 3.4 适配登录流程 | OPENID 自动获取 |
| 3.5 文件上传改造 | `wx.cloud.uploadFile` |

### 阶段四：Admin 云函数（3-5 天）

| 顺序 | 云函数 |
|------|--------|
| 1 | `admin-auth` |
| 2 | `admin-goods` |
| 3 | `admin-order` |
| 4 | `admin-marketing` |
| 5 | `admin-content` |
| 6 | `admin-clothing` |
| 7 | `admin-stat` |
| 8 | `admin-config` |
| 9 | `admin-user` |
| 10 | `admin-storage` |
| 11 | `admin-wework` |

### 阶段五：管理后台前端改造（3-5 天）

| 任务 | 说明 |
|------|------|
| 5.1 集成 JS SDK | 安装 + 初始化 |
| 5.2 认证改造 | CloudBase Auth 替代 Shiro |
| 5.3 API 调用层改造 | axios → callFunction |
| 5.4 文件上传改造 | OSS → 云存储 |
| 5.5 构建配置调整 | publicPath、hash 路由 |
| 5.6 部署到静态托管 | uploadFiles |

### 阶段六：收尾与验证（1-2 天）

| 任务 | 说明 |
|------|------|
| 6.1 定时任务云函数 | `task-order`、`task-notify` |
| 6.2 端到端测试 | 完整下单流程验证 |
| 6.3 数据一致性检查 | 旧数据迁移验证 |
| 6.4 阿里云资源清理 | 确认无遗漏后释放 |

---

## 10. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| **328 个 API 重写工作量大** | 整体工期长 | 分阶段交付，WX 端优先 |
| **云函数冷启动** | 首次请求慢（1-3 秒） | 预置并发 + 定时预热 + 函数分组减少数量 |
| **MySQL 连接数管理** | 云函数多实例可能耗尽连接数 | 共享连接池层 + connectionLimit=5 |
| **Java → Node.js 业务逻辑遗漏** | 功能回归 | 逐 Controller 对照，写测试用例 |
| **管理后台静态托管域名** | 平台默认域名有频率限制 | 后台访问量极低，默认域名完全够用 |
| **企微回调缺失** | 如果有回调场景无法接收 | 确认当前是否只用主动调用，文档显示只用了主动调用 |

---

## 11. 工作量估算

| 模块 | 估算 | 说明 |
|------|------|------|
| 基础设施搭建 | 1-2 天 | MySQL 迁移 + 层 |
| WX 云函数（~110 接口，去掉支付相关） | 4-6 天 | 核心业务，需逐个验证 |
| 小程序适配 | 2-3 天 | 改动相对小 |
| Admin 云函数（~200 接口） | 5-7 天 | CRUD 居多，可模板化 |
| 管理后台前端改造 | 3-5 天 | API 调用层 + 认证 + 文件上传 |
| 测试与收尾 | 2-3 天 | 端到端验证 |
| **总计** | **约 17-26 天** | 1 人全职 |

---

## 12. 已确认决策

| 决策项 | 结论 |
|--------|------|
| CloudBase 环境 | ✅ 已创建 |
| 小程序云开发 | ⏳ 待开通（迁移前需在小程序后台开通） |
| 微信支付 | 不需要迁移（当前未打通，后续按 CloudBase 支付模板接入即可） |
| 管理后台访问 | 使用 CloudBase 静态托管默认域名，低频访问，完全够用 |
| Node.js 重写 | ✅ 接受 |
| 阿里云环境 | 不保留，全量切换（当前 git 是镜像，原项目仍保留） |
