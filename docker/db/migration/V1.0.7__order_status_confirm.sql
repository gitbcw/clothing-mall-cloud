-- 新增订单状态 150（待确认）
-- 说明：用户付款后进入此状态，管理员确认后变为待发货/待自提
-- 注意：状态码需要与后端 OrderUtil.java 同步

-- 此迁移脚本仅作为文档记录，状态码定义在 Java 代码中
-- 现有订单不受影响，仅对新订单应用新流程

-- 订单状态说明：
-- 150: 待确认 - 用户已付款，等待管理员确认
-- 确认后：
--   - deliveryType=express -> 201(已付款/待发货) -> 301(已发货) -> 401/402(已收货)
--   - deliveryType=pickup -> 501(待核销) -> 502(已核销)
