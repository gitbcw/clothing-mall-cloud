{
  "name": "miniprogram-tests",
  "version": "1.0.0",
  "description": "小程序自动化测试",
  "scripts": {
    "test": "jest --runInBand",
    "test:home": "jest test_home.spec.js --runInBand",
    "test:catalog": "jest test_catalog.spec.js --runInBand",
    "test:goods": "jest test_goods.spec.js --runInBand",
    "test:cart": "jest test_cart.spec.js --runInBand",
    "test:user": "jest test_user.spec.js --runInBand",
    "test:floating-cart": "jest test_floating_cart.spec.js --runInBand",
    "test:order": "jest test_order.spec.js --runInBand",
    "test:verbose": "jest --verbose --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "miniprogram-automator": "^0.12.1"
  }
}
